// This program will make vairables and assign numbers to them
// then display them to the screen
#include <stdio.h>

int main(void)
{
    // Declare a variable x as an integer
    int x;

    // Assign a value to x
    x = 0;

    // Print the current value of x
    printf("x is %i\n", x);
}